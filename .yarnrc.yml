# Hoisted node_modules — best compatibility with Angular CLI, Husky, and IDEs.
# Yarn is the primary package manager; npm/pnpm/bun still work with their own lockfiles (not committed).
nodeLinker: node-modules
